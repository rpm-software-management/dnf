[aliases]

FORCE = --skip-broken --disableexcludes=all

up = upgrade
inst = install
in = install

ls = list
lsi = ls installed

force-install = FORCE install
force-inst = FORCE inst

h = history
cu = check-update

